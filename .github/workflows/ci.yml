# Copyright (c) Meta Platforms, Inc. and affiliates.
# All rights reserved.
#
# This source code is licensed under the license found in the
# LICENSE file in the root directory of this source tree.

name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  cpu-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      CUDA_VISIBLE_DEVICES: ""
      OMP_NUM_THREADS: "1"
      MKL_NUM_THREADS: "1"
    steps:
      - name: Check out UnStep
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Check out public Self Forcing source
        uses: actions/checkout@v4
        with:
          repository: guandeh17/Self-Forcing
          ref: main
          path: assets/source/Self-Forcing-main
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install public CPU dependencies
        run: |
          python -m pip install torch==2.8.0 --index-url https://download.pytorch.org/whl/cpu
          python -m pip install -e .

      - name: Check Python and shell syntax
        run: |
          python -m compileall -q unstep runtime evaluation tests
          for script in installation/*.sh generation/*.sh evaluation/*.sh benchmarks/*.sh; do bash -n "$script"; done

      - name: Run CPU arithmetic, contract, and safety tests
        run: python -m unittest discover -s tests -v
